Create image files with

`python .pio\libdeps\lilygo-t-display\lvgl\scripts\LVGLImage.py --ofmt C --cf I2 -o src/res assets\headlights.png `

Needs `pip install pypng lz4` and `pngquant` binary (https://pngquant.org/)
